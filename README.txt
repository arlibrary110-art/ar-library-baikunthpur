FRESH DOCKERFILE FOR AR LIBRARY

Replace only the repository's Dockerfile with the included Dockerfile.
Permanent attendance QR is not changed by this Dockerfile.

MongoDB PHP extension is pinned to 2.5.2, which satisfies mongodb/mongodb ^2.4.
