AR Library - Member Add / Fee Due Fix V6

Fixed files:
1. db.php
   - Added sync() to force dirty MongoDB tables to persist before success response.
2. members_api.php
   - Member + initial Pending Fee Due are now one atomic operation.
   - Initial due is mandatory and verified before success.
   - Fee amount reads fee_settings with safe fallback.
   - MongoDB sync runs before returning success.
   - Clean JSON response buffer prevents PHP warnings/output from corrupting JSON.
   - Added admin-only Repair Due action for a specific member when the Pending Due row is missing.
3. script.js
   - Add Member reads raw response and parses JSON safely.
   - Success message confirms Fee Due creation.
   - Admin member list has Repair Due button.

Expected result for a NEW member:
Member saved + one Pending member_fees row + Due Fee Members entry + Member App shows Due — amount.

Existing members:
Use the admin "Repair Due" button only for a member whose Pending Fee Due is actually missing. If a Pending row already exists, it will not create a duplicate.
