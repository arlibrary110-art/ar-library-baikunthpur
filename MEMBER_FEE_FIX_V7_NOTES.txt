AR Library Member Fee Due FIX V7

Main fix:
- Fixed MongoDB sync failure path in db.php.
- MongoDB transaction is attempted first; if transactions are unavailable, the adapter now falls back to direct delete/insert collection sync.
- This prevents the situation where the member record is persisted but the initial member_fees Pending record fails to persist because the Mongo endpoint does not support transactions.
- Added cache-busting version to index.php so the browser loads the new script.js.
- Removed unreachable response code from members_api.php.

Expected:
New Member -> Member saved -> Pending Fee Due created -> Due Members shows amount -> Member App shows Due amount until payment is collected.

Deploy the full ZIP, not individual files.
